@model VilaStella.WebAdminClient.Models.ForgotPasswordViewModel

@{
    ViewBag.Title = "Forgotten Password";
}

<div class="modal fade" id="modalWindow" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" style="position: absolute; left: 95%;" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title text-center" id="myModalLabel">Забравена Парола</h4>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("ForgottenPassword", "Account", FormMethod.Post, new { id = "modalForm", @class = "form-horizontal" }))
                {
                    @Html.AntiForgeryToken()

                    <div>
                        <p class="text-center">Моля, въведете адреса на електронната поща, с която сте се регистрирали. </p>
                        <p class="text-center">Ще получите писмо, в което се съдържат инструкции за създаване на нова парола.</p>
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(x => x.Email, "Email", new { @class = "col-md-4 control-label" })

                        <div class="col-md-5">
                            @Html.EditorFor(x => x.Email, new { htmlAttributes = new { @class = "form-control inpud-md" } })
                        </div>
                    </div>

                    <div class="row text-center">
                        <button class="btn btn-primary submit-button" data-loading-text="Изпрати">Изпрати</button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script src="~/Scripts/App/Shared/initialize-modal.js"></script>
    <script src="~/Scripts/App/Shared/validate-modal.js"></script>
}